{% extends "layout.html.j2" %}
{% block content %}
  <div class="row">
    <div class="col-sm-2 col-md-2"></div>
    <div class="col-sm-8 col-md-8">
      <h1 class="text-center">Log in</h1>
    </div>
    <div class="col-sm-2 col-md-2"></div>
  </div>
  <div class="content-section">
    <form action="" method="post" novalidate="novalidate">
      {{ form.hidden_tag() }}
      <fieldset class="form-group">
        <div class="form-group">
          {{ form.username.label(class="form-control-label") }}
          {% if form.username.errors %}
            {{ form.username(class="form-control form-control-lg is-invalid") }}
            <div class="invalid-feedback">
              {% for error in form.username.errors %}
                <span>{{ error }}</span>
              {% endfor %}
            </div>
          {% else %}
            {{ form.username(class="form-control form-control-lg") }}
          {% endif %}
        </div>
        <div class="form-group">
          {{ form.password.label(class="form-control-label") }} <small>(Note that passwords are case-sensitive.)</small>
          {% if form.password.errors %}
            {{ form.password(class="form-control form-control-lg is-invalid") }}
            <div class="invalid-feedback">
              {% for error in form.password.errors %}
                <span>{{ error }}</span>
              {% endfor %}
            </div>
          {% else %}
            {{ form.password(class="form-control form-control-lg") }}
          {% endif %}
        </div>
        <div class="form-group">
          {{ form.remember }} {{ form.remember.label }}
        </div>
      </fieldset>
      <div class="form-group">
        {{ form.submit(class="btn btn-primary") }}
      </div>
      <small class="text-muted">
        If you have forgotten your password please email <a href="mailto:neelu.rsa@gmail.com">neelu.rsa@gmail.com</a>.
      </small>
      <br>
      <small class="text-muted">
        Need an account? <a class="ml-2" href="{{ url_for('users.signup') }}">Sign up!</a>
      </small>
    </form>
  </div>
{% endblock content %}
